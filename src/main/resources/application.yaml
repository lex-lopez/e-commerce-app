spring:
  application:
    name: store
  datasource:
    url: ${MYSQL_URL}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}
    accessTokenTTL: 300 # 5m
    refreshTokenTTL: 604800 # 7d